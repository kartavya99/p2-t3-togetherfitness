    <div class="row">
      <div class="col-lg-8">
      <div class="col-12">
      <div class="section-title-sm">
      	<h3>Workout details</h3>
      </div>
      </div>
        <div class="workout-container">
         <div class="workout-content">
          <h2>{{workout.title}}</h2>
           <h3>{{workout.type}}</h3>
           <h4>Date: <span>{{workout.date}}</span></h4>
           <h4>Time: <span>{{workout.time}}</span></h4>
           <h4>Duration: <span>{{workout.duration}}</span></h4>
           {{workout.description}}
            <h3><i class="fa-solid fa-people-group me-2"></i> Attendees</h3>
            <div class="">
                <a href="#" class="btn btn-primary">Count me in!</a>          
            </div>
            <div class="">n
                <a href="#" class="btn btn-secondary">I can't make it anymore</a>          
            </div>
         </div>
		     </div>
      </div>
      <div class="col-lg-4">
          <div class="col-12">
      <div class="section-title-sm">
      	<h3>Your Host</h3>
      </div>
        <div class="host-info">
        <div class="inner">
              {{!-- <h4>{{user.firstname user.lastname}}</h4>
              <p>{{user.bio}}</p> --}}
        </div>
      </div>
              <div class="col-12">
      <div class="section-title-sm">
      	<h3>Location</h3>
      </div>
         <div class="group-map">
          <figure class="map-area" id="map">
             {{!-- <img src="/img/map-placeholder.jpg" alt="Map">  --}}
             </figure>
          <div class="map-content"> 
            <h4>{{workout.address}}</h4>
            <a href="#" class="read-more">View map</a> 
          </div>
        </div>
       </div>
    </div>
<script src="./js/workout.js"></script>